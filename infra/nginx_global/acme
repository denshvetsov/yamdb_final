# В общем случае для получения сертификата необходимо во всех блоках server 
# добавить следующий блок до других блоков location:
# Понятно, что вписывать для каждого сайта такой блок явно — это моветон, 
# потому создадим файл /etc/nginx/acme с содержанием блока ниже.

# cat /etc/nginx/acme
location /.well-known {
    root /var/www/letsencrypt;
}

# Затем для каждого домена и поддомена, для которых нужно получить сертификаты, 
# в блоке server перед всеми блоками location укажем
#include acme;